import React from 'react';
import ReactModal from "react-modal";

ReactModal.setAppElement(document.body);
function App() {
    return (
        <div className="App">
        </div>
    );
}

export default App;
